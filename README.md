# Image-Steganography
A simple LSB-based steganography tool that hides one image inside another and extracts it back without loss. The notebook shows the full process: embedding, generating the stego image, and recovering the hidden image, with minimal visual distortion.
